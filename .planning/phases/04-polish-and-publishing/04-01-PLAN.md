---
phase: 04-polish-and-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - pubspec.yaml
  - lib/src/controller/ai_chat_scroll_controller.dart
  - lib/src/widgets/ai_chat_scroll_view.dart
  - lib/ai_chat_scroll.dart
autonomous: true
requirements: [QUAL-01, QUAL-03, QUAL-05]

must_haves:
  truths:
    - "README contains a copy-paste-ready integration example showing AiChatScrollView with itemBuilder/itemCount API"
    - "README describes the core value proposition (anchor-on-send, not auto-scroll)"
    - "CHANGELOG has entries for all implemented features across phases 1-3"
    - "Every public symbol has a dartdoc comment (no undocumented public API warnings from dart doc)"
    - "pubspec.yaml declares supported platforms and has a valid description"
  artifacts:
    - path: "README.md"
      provides: "Developer-facing documentation with integration example"
      contains: "AiChatScrollView"
    - path: "CHANGELOG.md"
      provides: "Version history for 0.1.0 release"
      contains: "onUserMessageSent"
    - path: "pubspec.yaml"
      provides: "Package metadata with platforms declaration"
      contains: "platforms"
  key_links:
    - from: "README.md"
      to: "lib/ai_chat_scroll.dart"
      via: "code example matches actual public API"
      pattern: "AiChatScrollView.*itemBuilder.*itemCount"
---

<objective>
Polish all package metadata files (README, CHANGELOG, pubspec) and audit dartdoc coverage on all public APIs for pub.dev readiness.

Purpose: QUAL-01 (pub.dev structure), QUAL-03 (dartdoc), and QUAL-05 (pana score) require complete metadata and documentation. The README is outdated (still says "Phase 1 complete"), CHANGELOG only covers Phase 1, and pubspec lacks the `platforms` key needed for full pub points.
Output: Updated README.md, CHANGELOG.md, pubspec.yaml, and any dartdoc additions to source files.
</objective>

<execution_context>
@/Users/bommel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bommel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md

@lib/src/controller/ai_chat_scroll_controller.dart
@lib/src/widgets/ai_chat_scroll_view.dart
@lib/ai_chat_scroll.dart
@pubspec.yaml
@README.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README, CHANGELOG, and pubspec metadata</name>
  <files>README.md, CHANGELOG.md, pubspec.yaml</files>
  <action>
**README.md** — Complete rewrite. Structure:
1. Package name + one-line description
2. "Problem" section (1 paragraph): Auto-scroll during AI streaming disorients users
3. "Solution" section (1 paragraph): This package anchors the user's sent message at the top while the response grows below
4. "Installation" section: `flutter pub add ai_chat_scroll`
5. "Quick Start" section: Copy-paste-ready Dart code showing a StatefulWidget with:
   - `AiChatScrollController` creation and disposal
   - `AiChatScrollView` with `controller`, `itemCount`, and `itemBuilder` parameters
   - `controller.onUserMessageSent()` call after adding user message to list
   - `controller.onResponseComplete()` call when AI finishes
   - Comments explaining what each call does
6. "How It Works" section (3-4 bullet points): Anchor-on-send, filler shrinks as response grows, user drag cancels anchor, no auto-scroll during streaming
7. "API Reference" section: Brief table of `AiChatScrollController` methods (`onUserMessageSent()`, `onResponseComplete()`, `isStreaming`, `dispose()`) and `AiChatScrollView` constructor params (`controller`, `itemBuilder`, `itemCount`)
8. "Requirements" section: Flutter >= 3.22.0, zero runtime dependencies
9. Remove all "Phase X" status tracking — this is a published package now, not a dev log.
10. Do NOT include badges, emojis, or shields.io links.

**CHANGELOG.md** — Rewrite for 0.1.0 release:
```
## 0.1.0

- `AiChatScrollController` with `onUserMessageSent()` and `onResponseComplete()` lifecycle methods
- `AiChatScrollView` widget with `itemBuilder`/`itemCount` builder API
- Top-anchor-on-send: user's message snaps to viewport top when sent
- Streaming filler: dynamic space below AI response keeps anchor stable during streaming
- User drag cancellation: dragging during streaming cancels managed scroll
- Zero runtime dependencies (Flutter SDK only)
- Supports iOS (bouncing) and Android (clamping) scroll physics via ambient ScrollConfiguration
```

**pubspec.yaml** — Add the `flutter.platforms` key for pub points:
```yaml
flutter:
  platforms:
    android:
    ios:
```
Keep the existing `topics`, `homepage`, `description`, `environment`, and `dependencies` sections unchanged. Do NOT change the version (keep 0.1.0). Keep `homepage` as-is (user will update before actual publish).
  </action>
  <verify>
    <automated>/Users/bommel/flutter/flutter/bin/dart doc --dry-run . 2>&1 | tail -5 && /Users/bommel/flutter/flutter/bin/dart analyze --no-pub 2>&1 | tail -3</automated>
  </verify>
  <done>README has integration example with AiChatScrollView(itemBuilder/itemCount), CHANGELOG has full 0.1.0 feature list, pubspec has platforms key, dart analyze still passes with 0 warnings</done>
</task>

<task type="auto">
  <name>Task 2: Audit and complete dartdoc coverage on all public APIs</name>
  <files>lib/src/controller/ai_chat_scroll_controller.dart, lib/src/widgets/ai_chat_scroll_view.dart, lib/ai_chat_scroll.dart</files>
  <action>
Audit every public symbol for dartdoc completeness. The existing dartdoc is already good — this task verifies and fills any gaps.

**Audit checklist (read each file, verify each public symbol has a `///` comment):**

1. `AiChatScrollController` class — has dartdoc (verify)
2. `AiChatScrollController.isStreaming` getter — has dartdoc (verify)
3. `AiChatScrollController.attach()` — has dartdoc (verify)
4. `AiChatScrollController.detach()` — has dartdoc (verify)
5. `AiChatScrollController.onUserMessageSent()` — has dartdoc (verify)
6. `AiChatScrollController.onResponseComplete()` — has dartdoc (verify)
7. `AiChatScrollController.dispose()` — MISSING dartdoc override. Add: `/// Releases resources and detaches from any [ScrollController].\n/// \n/// Call this when the controller is no longer needed. After disposal,\n/// do not call any other methods on this controller.`
8. `AiChatScrollView` class — has dartdoc (verify)
9. `AiChatScrollView.controller` field — has dartdoc (verify)
10. `AiChatScrollView.itemBuilder` field — has dartdoc (verify)
11. `AiChatScrollView.itemCount` field — has dartdoc (verify)
12. `lib/ai_chat_scroll.dart` barrel — has `library` directive. Verify it has a library-level dartdoc comment. If missing, add: `/// AI-chat-optimized scroll behavior for Flutter.\n///\n/// This library provides [AiChatScrollController] and [AiChatScrollView]\n/// to implement the top-anchor-on-send pattern: when a user sends a message,\n/// it snaps to the top of the viewport and the AI response grows below.`

Run `dart doc .` after changes and verify 0 warnings about undocumented public APIs.
  </action>
  <verify>
    <automated>/Users/bommel/flutter/flutter/bin/dart doc . 2>&1 | grep -i "warning\|error" | head -10; echo "EXIT: $?"</automated>
  </verify>
  <done>Every public symbol (classes, methods, getters, fields) in the barrel export has a dartdoc comment. `dart doc` generates with 0 undocumented-API warnings.</done>
</task>

</tasks>

<verification>
- `dart analyze` reports 0 issues
- `dart doc .` (or `dart doc --dry-run .`) reports 0 undocumented public API warnings
- README.md contains `AiChatScrollView` with `itemBuilder` and `itemCount` in a code block
- CHANGELOG.md mentions `onUserMessageSent`, `onResponseComplete`, and `AiChatScrollView`
- pubspec.yaml contains `platforms:` key with `android:` and `ios:`
</verification>

<success_criteria>
All package metadata files are pub.dev-ready. Every public API symbol has dartdoc. dart analyze passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-publishing/04-01-SUMMARY.md`
</output>
