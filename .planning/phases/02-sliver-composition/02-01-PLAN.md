---
phase: 02-sliver-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/widgets/ai_chat_scroll_view.dart
  - lib/src/widgets/filler_sliver.dart
  - test/ai_chat_scroll_controller_test.dart
  - test/ai_chat_scroll_view_test.dart
autonomous: true
requirements: [API-03, SCRL-01, SCRL-02, SCRL-03, SCRL-04]

must_haves:
  truths:
    - "AiChatScrollView accepts itemBuilder and itemCount instead of child"
    - "Messages render in a CustomScrollView with SliverList.builder"
    - "A FillerSliver exists below the message list, isolated via ValueNotifier"
    - "No physics parameter is hardcoded -- ambient ScrollConfiguration is inherited"
    - "Scroll position (pixels) does not change when a message is inserted and user is mid-list"
    - "All Phase 1 tests still pass after API migration"
  artifacts:
    - path: "lib/src/widgets/ai_chat_scroll_view.dart"
      provides: "AiChatScrollView with itemBuilder/itemCount API, CustomScrollView + SliverList.builder + FillerSliver"
      contains: "itemBuilder"
    - path: "lib/src/widgets/filler_sliver.dart"
      provides: "FillerSliver widget with ValueListenableBuilder isolation"
      contains: "ValueListenableBuilder"
    - path: "test/ai_chat_scroll_view_test.dart"
      provides: "Widget tests for sliver composition (API-03, SCRL-01 through SCRL-04)"
      min_lines: 50
  key_links:
    - from: "lib/src/widgets/ai_chat_scroll_view.dart"
      to: "lib/src/widgets/filler_sliver.dart"
      via: "FillerSliver widget instantiation in slivers list"
      pattern: "FillerSliver\\(fillerHeight:"
    - from: "lib/src/widgets/ai_chat_scroll_view.dart"
      to: "ValueNotifier<double>"
      via: "_fillerHeight ValueNotifier created in initState, passed to FillerSliver"
      pattern: "ValueNotifier<double>"
    - from: "test/ai_chat_scroll_view_test.dart"
      to: "lib/src/widgets/ai_chat_scroll_view.dart"
      via: "Widget tests exercising itemBuilder/itemCount API"
      pattern: "itemBuilder.*itemCount"
---

<objective>
Implement sliver composition in AiChatScrollView: replace the Phase 1 `child: Widget` stub with an `itemBuilder`/`itemCount` builder API backed by `CustomScrollView` + `SliverList.builder` + an isolated `FillerSliver`. Update existing tests for the breaking API change and create new widget tests covering all Phase 2 requirements.

Purpose: This is the rendering foundation for the top-anchor-on-send behavior. The package must own the CustomScrollView to compose slivers (message list + filler). The FillerSliver isolation via ValueNotifier prevents full-list rebuilds when filler height changes during streaming (Phase 3).

Output: Working AiChatScrollView with builder API, FillerSliver widget, updated Phase 1 tests, new Phase 2 widget tests -- all green.
</objective>

<execution_context>
@/Users/bommel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bommel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sliver-composition/02-RESEARCH.md
@.planning/phases/01-controller-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan depends on. -->

From lib/src/controller/ai_chat_scroll_controller.dart:
```dart
class AiChatScrollController extends ChangeNotifier {
  ScrollController? _scrollController;
  void attach(ScrollController scrollController);
  void detach();
  void onUserMessageSent();
  void onResponseComplete();
  void dispose();
}
```

From lib/src/widgets/ai_chat_scroll_view.dart (CURRENT -- will be replaced):
```dart
class AiChatScrollView extends StatefulWidget {
  const AiChatScrollView({
    super.key,
    required this.controller,
    required this.child,  // REMOVING -- replace with itemBuilder/itemCount
  });
  final AiChatScrollController controller;
  final Widget child;
}
```

From lib/ai_chat_scroll.dart (barrel -- NO CHANGES needed):
```dart
library ai_chat_scroll;
export 'src/controller/ai_chat_scroll_controller.dart';
export 'src/widgets/ai_chat_scroll_view.dart';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FillerSliver and migrate AiChatScrollView to builder API with sliver composition</name>
  <files>lib/src/widgets/filler_sliver.dart, lib/src/widgets/ai_chat_scroll_view.dart, test/ai_chat_scroll_controller_test.dart</files>
  <action>
1. Create `lib/src/widgets/filler_sliver.dart`:
   - A `StatelessWidget` named `FillerSliver` (internal, not exported via barrel).
   - Constructor takes `required ValueNotifier<double> fillerHeight`.
   - `build()` returns `ValueListenableBuilder<double>` wrapping a `SizedBox(height: height)`.
   - Add dartdoc: internal widget for dynamic filler space, isolated from message list rebuilds.

2. Rewrite `lib/src/widgets/ai_chat_scroll_view.dart`:
   - Replace `child: Widget` parameter with `required IndexedWidgetBuilder itemBuilder` and `required int itemCount`.
   - Update dartdoc: remove child references, document itemBuilder/itemCount, update usage example to show builder pattern.
   - In `_AiChatScrollViewState`:
     - Add `late final ValueNotifier<double> _fillerHeight;` field.
     - In `initState()`: create `_fillerHeight = ValueNotifier(0.0)` (Phase 2: always zero; Phase 3 will drive it).
     - In `dispose()`: call `_fillerHeight.dispose()` BEFORE `_scrollController.dispose()`.
     - `build()` returns `CustomScrollView` with:
       - `controller: _scrollController`
       - NO `physics:` parameter (inherit ambient ScrollConfiguration -- SCRL-04)
       - NO `reverse: true` (forward-growing coordinate system)
       - `slivers:` list containing:
         1. `SliverList.builder(itemCount: widget.itemCount, itemBuilder: widget.itemBuilder)`
         2. `SliverToBoxAdapter(child: FillerSliver(fillerHeight: _fillerHeight))`
   - Import `filler_sliver.dart` at top.

3. Update `test/ai_chat_scroll_controller_test.dart`:
   - In the `AiChatScrollView lifecycle` group, replace all `child: const SizedBox()` with `itemBuilder: (_, __) => const SizedBox.shrink(), itemCount: 0`.
   - This is the breaking API migration. Phase 1 tests must still pass after this change.

IMPORTANT: Do NOT add `physics:` to CustomScrollView. Do NOT use `reverse: true`. Do NOT call `jumpTo` or `scrollTo` anywhere. Do NOT export FillerSliver from the barrel.
  </action>
  <verify>
    <automated>cd /Users/bommel/Project/Better_chat_scrolling && export PATH="/Users/bommel/flutter/flutter/bin:$PATH" && flutter test test/ai_chat_scroll_controller_test.dart</automated>
  </verify>
  <done>FillerSliver widget exists with ValueListenableBuilder isolation. AiChatScrollView uses itemBuilder/itemCount API with CustomScrollView + SliverList.builder + SliverToBoxAdapter(FillerSliver). All Phase 1 tests pass with updated API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create widget tests for sliver composition covering API-03, SCRL-01 through SCRL-04</name>
  <files>test/ai_chat_scroll_view_test.dart</files>
  <behavior>
    - API-03: AiChatScrollView renders with itemBuilder/itemCount and produces scrollable output containing the built items
    - SCRL-01: Given 5 messages passed in reverse order (index 0 = newest), scrolling to the end shows the newest message at the bottom of the rendered list
    - SCRL-02: Filler height change via ValueNotifier does NOT trigger a rebuild of message list items (verify via a builder call counter that does not increment when filler changes)
    - SCRL-03: When user is scrolled to mid-list (not at bottom) and itemCount increases by 1, scroll position in pixels remains unchanged
    - SCRL-04: CustomScrollView has no explicit physics parameter set (verify by finding the CustomScrollView in the widget tree and checking that its physics property is null)
  </behavior>
  <action>
Create `test/ai_chat_scroll_view_test.dart` with the following test groups:

1. **API-03: Builder API renders messages**
   - Create AiChatScrollView with controller, itemBuilder returning `SizedBox(height: 100, child: Text('Msg $index'))`, itemCount: 5.
   - Wrap in `MaterialApp` (provides Directionality + MediaQuery + ScrollConfiguration).
   - Pump and verify all 5 message texts are findable in the widget tree.

2. **SCRL-01: Newest message at bottom**
   - Create AiChatScrollView with 10 messages (index 0 = "newest", index 9 = "oldest").
   - Each item is `SizedBox(height: 100, child: Text('Msg $index'))`.
   - After pump, verify that `find.text('Msg 0')` (newest, at top of sliver = bottom of visual list) is in the tree.
   - Use `tester.getTopLeft(find.text('Msg 0'))` and `tester.getTopLeft(find.text('Msg 9'))` to verify Msg 0 has a SMALLER Y offset than Msg 9 would if scrolled into view (or verify the ordering of visible items).
   - Note: In a forward-growing list with reversed data, index 0 renders at the top of the sliver. The "newest at bottom" visual is achieved by the consumer reversing their data. In the test, verify that items render in index order (0 at top, 9 at bottom of scrollable area). The consumer reversal is documented, not tested here.

3. **SCRL-02: Filler isolation (no full-list rebuild)**
   - This test requires accessing the internal ValueNotifier. Since FillerSliver is internal, test indirectly:
   - Create AiChatScrollView with 3 messages. Use a `buildCount` integer incremented each time itemBuilder is called.
   - After initial pump, record `buildCount`.
   - Force a widget rebuild by calling `tester.pumpWidget(...)` again with the SAME itemCount and itemBuilder (simulating a filler-only change scenario).
   - Verify that `buildCount` did not increase beyond what SliverList.builder normally rebuilds (i.e., the builder is not called again for all items on a no-op rebuild).
   - Also verify: Find `CustomScrollView` in tree, find `SliverToBoxAdapter` in tree (proves filler sliver exists in composition).

4. **SCRL-03: Scroll position preserved on insertion**
   - Create AiChatScrollView with 20 messages, each 100px tall, in a 600px viewport.
   - Scroll to mid-list using `controller` (find the internal ScrollController via `tester.widget<CustomScrollView>(find.byType(CustomScrollView)).controller` and call `jumpTo(500)`).
   - Pump to settle.
   - Record `pixels` value.
   - Rebuild with itemCount: 21 (one new message).
   - Pump to settle.
   - Verify `pixels` value is unchanged (SCRL-03: no automatic jumpTo on insertion).

5. **SCRL-04: No hardcoded physics**
   - Create AiChatScrollView with 3 messages.
   - Find `CustomScrollView` widget in tree.
   - Assert that `customScrollView.physics` is `null` (inherits ambient).

Each test must create and dispose its own AiChatScrollController.

Run command: `flutter test test/ai_chat_scroll_view_test.dart`
  </action>
  <verify>
    <automated>cd /Users/bommel/Project/Better_chat_scrolling && export PATH="/Users/bommel/flutter/flutter/bin:$PATH" && flutter test test/ai_chat_scroll_view_test.dart</automated>
  </verify>
  <done>All 5 test groups pass. API-03 confirmed (builder API renders items). SCRL-01 confirmed (items render in index order). SCRL-02 confirmed (filler sliver exists in composition, isolated). SCRL-03 confirmed (scroll pixels unchanged on insertion when mid-list). SCRL-04 confirmed (no hardcoded physics on CustomScrollView).</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to confirm no regressions:

```bash
cd /Users/bommel/Project/Better_chat_scrolling && export PATH="/Users/bommel/flutter/flutter/bin:$PATH" && flutter test
```

Also run static analysis:
```bash
cd /Users/bommel/Project/Better_chat_scrolling && export PATH="/Users/bommel/flutter/flutter/bin:$PATH" && flutter analyze
```

Both must pass with zero errors and zero warnings.
</verification>

<success_criteria>
- AiChatScrollView uses itemBuilder/itemCount API (child: Widget removed entirely)
- CustomScrollView + SliverList.builder + SliverToBoxAdapter(FillerSliver) composition in build()
- FillerSliver uses ValueListenableBuilder for isolation (ValueNotifier<double> initialized to 0.0)
- No physics: parameter on CustomScrollView
- All Phase 1 controller tests pass with updated API
- New widget tests cover API-03, SCRL-01, SCRL-02, SCRL-03, SCRL-04
- flutter test passes (full suite green)
- flutter analyze passes with zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-sliver-composition/02-01-SUMMARY.md`
</output>
